# LangGraph execution + durable checkpoint backends for AI Army.
# Kept separate from the native runtime so the framework remains replaceable.
langgraph==1.2.11
langgraph-checkpoint-sqlite==3.1.1
langgraph-checkpoint-postgres==3.1.2
